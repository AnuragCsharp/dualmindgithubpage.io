
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  
  distDir: 'nextbuild', // Set the custom output directory here
  basePath: '/DualmindWebbuild', // Set the base path to your repository name
};

export default nextConfig;
